# Repository Workflow

After completing a requested coding task, run the narrowest relevant validation, commit the completed changes with a concise message, and push the commit to `origin` on the current branch. Do not push if validation fails or if the user explicitly asks to pause before pushing. Never include secrets in commits.
